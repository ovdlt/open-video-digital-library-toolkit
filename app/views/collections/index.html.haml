.collections

  .title= @title

  .contents

    - if @subtitle
      .pagination_info= @subtitle
    - else
      %div &nbsp;

    = render :partial => "collections/#{partial}", :collection => @collections

    .pagination
      = will_paginate @collections
