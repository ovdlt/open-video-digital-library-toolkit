- form_for @video do |@f|

  .form

    / = debug(@video)

    .assets.current
    - if @video.assets.size > 0
      Current Assets
    - else
      No Current Assets

    .assets.new
      Add Assets
      = render :partial => "/assets/new"

  = @f.submit "Create new video"
