%fieldset

  = debug(@video)

  %legend
    = @video.new_record? ? "New" : "Edit"
    Video
  - if !error_messages_for( :video ).blank?
    .flash.error
      = error_messages_for :video
  .tabs
    %ul
      = tab_for :general_information
      = tab_for :digital_files
      = tab_for :responsible_entities
      = tab_for :dates
      = tab_for :chapters
      = tab_for :descriptors
      = tab_for :collections
      = tab_for :related_videos

